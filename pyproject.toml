[project]
name = "codebase"
version = "0.1.0"
description = "Code semantic database for AI agents"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=1.4.1",
    "ollama>=0.6.1",
    "typer>=0.21.1",
]
